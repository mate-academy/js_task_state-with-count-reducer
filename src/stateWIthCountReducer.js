'use strict';

/**
 * Implement countReducer function:
 *
 * Examples: initialState = { count: 5, x: 1 }
 * countReducer(initialState, { type: increase }) === { count: 6, x: 1}
 * countReducer(initialState, { type: decrease }) === { count: 4, x: 1}
 * countReducer(initialState, { type: clear }) === { count: 0, x: 1}
 * countReducer(initialState, { type: fdafad }) === { count: 5, x: 1}
 *
 *
 * @param {object} state
 * @param {object} action
 *
 * @return {object}
 */

const countReducer = () => {
  // write code here (remove this text)
};

module.exports = countReducer;
